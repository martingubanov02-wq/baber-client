<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= typeof title !== 'undefined' ? title : 'BABER client' %></title>
  <link rel="stylesheet" href="/static/style.css" />
  <!-- deploy: v-hwid-16 -->
</head>
<body>
  <header class="wrap">
    <div class="nav">
      <a class="brand" href="/">BABER client</a>
      <nav>
        <% if (user) { %>
          <span class="welcome">👋 <%= user.username %></span>
          <a class="btn small" href="/hwid">HWID</a>
          <form method="post" action="/logout" style="display:inline">
            <button class="btn small" type="submit">Выйти</button>
          </form>
        <% } else { %>
          <a class="btn small" href="/login">Войти</a>
          <a class="btn small" href="/register">Регистрация</a>
        <% } %>
      </nav>
    </div>
  </header>
  <main class="wrap">
    <% if (flash) { %>
      <div class="flash <%= flash.type %>"><%= flash.message %></div>
    <% } %>
    
